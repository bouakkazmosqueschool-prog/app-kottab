-- ============================================================
-- صورة التلميذ مخزَّنة في قاعدة البيانات (صورة مصغَّرة بصيغة data URL)
--
-- يضيف عموداً نصياً photo على جدول التلاميذ (يحتوي data:image/jpeg;base64,...).
-- تُصغَّر الصورة في المتصفح قبل الحفظ لإبقاء الحجم صغيراً.
-- لا يمسّ أي بيانات موجودة. الاستعمال: SQL Editor → Run. idempotent.
-- ============================================================
alter table students add column if not exists photo text;
