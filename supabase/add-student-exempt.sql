-- ============================================================
-- تلاميذ معفَوْن من الأداء الشهري (لا يُطالَبون بالدفع)
--
-- يضيف عموداً منطقياً exempt على جدول التلاميذ (القيمة الافتراضية false).
-- لا يمسّ أي بيانات موجودة. الاستعمال: SQL Editor → Run. idempotent.
-- ============================================================
alter table students add column if not exists exempt boolean not null default false;
